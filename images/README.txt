# Drop figure files here.
#
# Required: ista-logo.pdf  (or .png) — referenced from frontmatter/cover.tex
#
# Prefer vector formats (PDF, SVG converted to PDF) over raster (PNG, JPG).
# Reference figures from the document with:
#   \includegraphics[width=0.8\textwidth]{filename}
# (no extension — graphicspath{{images/}} is already set in main.tex)
